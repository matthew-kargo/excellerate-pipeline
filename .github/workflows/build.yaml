---
name: Run Unit Tests

on: 
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v1
      - name: Build doker image
        run: |
          cd node
          docker build -t temp/build .
        