---
name: Build Docker Image

on: 
  push:
    branches:
      - master

jobs:
  build: 
    runs-on: ubuntu-latest
    
    steps:
      name: Build Application
      steps:
        - name: Checkout code
          uses: actions/checkout@v2
        - run: echo Continue here...
